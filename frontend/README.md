# noctoCRM Frontend

🎨 Frontend для noctoCRM - CRM-системы для малого бизнеса.

## Технологии

- **Next.js 15** - React фреймворк
- **TypeScript** - типизация
- **Tailwind CSS** - стили
- **Axios** - HTTP клиент

## Дизайн

Используется комбинация стилей:
- **Brutalism** - грубые рамки, тени, monospace шрифт
- **Glassmorphism** - прозрачные карточки с blur

### Цвета:
- Черный фон: `#0A0A0A`
- Белый: `#F5F5F5`
- Серый: `#D4D4D4`
- Акцент (оранжевый): `#FF6B35`

## Установка

### 1. Установите зависимости

```bash
cd frontend
npm install
```

### 2. Создайте `.env`

```bash
cp .env.example .env
```

Отредактируйте `.env`:

```env
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
```

### 3. Запустите разработку

```bash
npm run dev
```

🔗 Приложение: http://localhost:3000

## Структура

```
frontend/
├── src/
│   ├── app/
│   │   ├── login/         # Страница входа
│   │   ├── kanban/        # Kanban доска
│   │   ├── globals.css    # Глобальные стили
│   │   └── layout.tsx
│   └── lib/
│       └── api.ts         # API клиент
├── package.json
└── tailwind.config.js
```

## Страницы

### 1. `/login` - Авторизация
- Форма входа
- JWT аутентификация
- Хранение токена в localStorage

### 2. `/kanban` - Воронка продаж
- Kanban доска со стадиями
- Drag & Drop сделок
- Статистика по стадиям
- Выбор воронки

## Функционал

### Авторизация:
- ✅ Вход по username/password
- ✅ JWT токен
- ✅ Автоматический logout при 401

### Kanban доска:
- ✅ Просмотр всех стадий
- ✅ Drag & Drop перемещение сделок
- ✅ Количество сделок по стадиям
- ✅ Сумма сделок по стадиям
- ✅ Цветовое кодирование стадий

## API Интеграция

Все запросы к API проходят через `src/lib/api.ts`.

Автоматически добавляется:
- `Authorization` заголовок с JWT токеном
- Перенаправление на `/login` при 401 ошибке

## Разработка

### Dev сервер
```bash
npm run dev
```

### Build
```bash
npm run build
```

### Запуск production
```bash
npm run start
```

## Стили

### Brutalism классы:
- `.brutal-box` - контейнер с рамкой и тенью
- `.brutal-button` - основная кнопка
- `.brutal-button-secondary` - второстепенная кнопка
- `.brutal-input` - поле ввода

### Glassmorphism:
- `.glass-card` - прозрачная карточка с blur

### Kanban:
- `.kanban-column` - колонка стадии
- `.kanban-card` - карточка сделки (draggable)

## Поддерживаемые браузеры

- Chrome/Edge (latest)
- Firefox (latest)
- Safari (latest)
